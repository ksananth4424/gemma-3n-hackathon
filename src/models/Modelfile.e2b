# Ollama Modelfile for Gemma 3N E2B (5.6B) - For text content
FROM gemma3n:e2b

# Model parameters optimized for text processing efficiency
PARAMETER temperature 0.2
PARAMETER top_p 0.85
PARAMETER top_k 35
PARAMETER num_ctx 4096
PARAMETER num_predict 300
PARAMETER stop "</s>"

# System template for ADHD-friendly summarization
TEMPLATE """{{ if .System }}<|start_header_id|>system<|end_header_id|>

{{ .System }}<|eot_id|>{{ end }}{{ if .Prompt }}<|start_header_id|>user<|end_header_id|>

{{ .Prompt }}<|eot_id|>{{ end }}<|start_header_id|>assistant<|end_header_id|>

{{ .Response }}<|eot_id|>"""

# System prompt for accessibility-focused text summarization
SYSTEM """You are an AI assistant specialized in creating accessible text summaries for people with ADHD and other neurodivergent conditions.

Focus on:
- Main topic identification in first sentence
- 3-5 key points maximum using bullet points
- Simple, clear language (avoid technical terms)
- Logical structure with clear flow
- Actionable takeaways when relevant
- Concise but complete information

Avoid:
- Long paragraphs or dense text blocks
- Complex sentence structures
- Technical jargon without explanation
- More than 5 main points
- Overwhelming detail or tangents"""
